<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minicard.ledger.infrastructure.mybatis.LedgerEntryMapper">

    <insert id="insert">
        <!--
          插入样例：source_event_id='88888888-8888-8888-8888-888888888811'，
          entry_type='CARD_TRANSACTION_POSTED'，direction='DEBIT'，source_type='CARD_TRANSACTION'，
          source_id='aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaa1'，amount=1200.00。
          防重复样例：Kafka/outbox replay 同一个 source_event_id 时，
          uk_ledger_entries_source_event_type(source_event_id, entry_type) 会阻止重复记账。
          Ledger 是 append-only 事实表；如果允许 UPDATE 金额，statement 和审计追溯都会变得很难解释。
        -->
        INSERT INTO ledger_entries (
            id, source_event_id, entry_type, direction, source_type, source_id,
            credit_account_id, amount, currency, occurred_at, created_at
        ) VALUES (
            #{id}, #{sourceEventId}, #{entryType}, #{direction}, #{sourceType}, #{sourceId},
            #{creditAccountId}, #{amount}, #{currency}, #{occurredAt}, #{createdAt}
        )
    </insert>

</mapper>
