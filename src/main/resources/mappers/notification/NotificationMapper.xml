<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minicard.notification.infrastructure.mybatis.NotificationMapper">

    <insert id="insert">
        INSERT INTO notifications (
            id, source_event_id, authorization_id, card_id, template, status,
            delivery_attempts, last_error, sent_at, created_at, updated_at
        ) VALUES (
            #{id}, #{sourceEventId}, #{authorizationId}, #{cardId}, #{template}, #{status},
            #{deliveryAttempts}, #{lastError}, #{sentAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

</mapper>
