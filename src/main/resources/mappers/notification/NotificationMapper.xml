<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minicard.notification.infrastructure.mybatis.NotificationMapper">

    <insert id="insert">
        <!--
          source_event_id 唯一键让通知创建具备第二道幂等保护。
          sent_at/last_error 可能为 null；如果后续 DB 对 null 类型判断失败，需要显式加 jdbcType。
        -->
        INSERT INTO notifications (
            id, source_event_id, subject_type, subject_id, recipient_key, template, status,
            delivery_attempts, last_error, sent_at, created_at, updated_at
        ) VALUES (
            #{id}, #{sourceEventId}, #{subjectType}, #{subjectId}, #{recipientKey}, #{template}, #{status},
            #{deliveryAttempts}, #{lastError}, #{sentAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

</mapper>
